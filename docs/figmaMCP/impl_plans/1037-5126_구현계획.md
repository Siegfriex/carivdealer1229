# 구현 계획: 1037-5126 검차요청내역 (동일페이지·상태1)

**대상 노드**: 1037:5126  
**IA 화면 라벨**: 매물등록 CTA_2 검차 — §3.6_1425-9445_검차요청내역_리스트_변형/리스트/카드뷰 [동일페이지], GNB 검차탭 직속  
**라우트**: `/inspections`  
**페이지 컴포넌트**: InspectionListPage

---

## 1. 목표

- Figma 노드 1037-5126(검차요청내역 디자인 상태 1)과 로컬 `/inspections` InspectionListPage를 1:1로 맞춘다. 리스팅 데이터 컨테이너 클릭 시 하단이 매칭단계/완료단계/결과(내역) 단계 중 하나로 전환·상태저장되는 동작을 디자인대로 반영한다.

---

## 2. 변경 대상 파일

- `src/pages/admin/inspection/InspectionListPage.tsx` — 레이아웃·리스트/카드뷰·하단 패널 전환·스타일 (mcp_outputs 채움 후 상세 적용)
- `docs/figmaMCP/NODE_TO_ROUTE_AND_FILE.md` — 1037-5126 매핑(완료)

---

## 3. 의존성

- **라우트**: `router.tsx` `/inspections` (ProtectedRoute 내부)
- **위젯**: LandingHeader (activeNav="inspections"), GnbMinimalSidebar, SegmentedControl, InspectionStatusBadge
- **z-index**: 헤더, 확장 패널
- **공통 스타일**: LAYOUT_CLASSES, design-tokens.css
- **API/features**: MOCK_INSPECTIONS(mockInspectionList), 상태별 필터·펼치기

---

## 4. MCP 스타일 → 프로젝트 디자인 토큰 매핑

- **mcp_outputs/1037-5126/design_context_raw.txt 채움 후** 추출하여 매핑.

---

## 5. 레이아웃 스펙 (노드별 위치·크기)

- **현재 mcp_outputs/1037-5126/metadata_raw.txt·design_context_raw.txt가 비어 있음.**  
  사용자가 1번 에이전트로 get_metadata·get_design_context 반환을 붙여넣은 뒤, 본 섹션을 노드 id별 (x, y, width, height) 테이블로 보강할 것.

| nodeId | x | y | width | height | 비고 |
|--------|---|---|-------|--------|------|
| (mcp_outputs 채움 후 보강) | | | | | |

---

## 6. 위험/주의사항

- 리스트/카드 뷰 전환, 상태 필터(전체·매칭중·매칭완료·검차중·검차완료 등), 행 클릭 시 확장·진행하기(/inspections/:id/progress) 동작 유지.
- 1037-5673, 1042-4681과 동일 페이지의 서로 다른 디자인 상태이므로, 3개 노드 디자인 반영 시 하단 전환 UI를 통일하여 구현.

---

*문서 버전: 1.0 | 최종 업데이트: 2025-02-10*
